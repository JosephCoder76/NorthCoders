function getAverage(marks){

  let counter = 0;
  let elements = 0;
  let result =0;
  
  for (i=0; i<marks.length; i++){
  
    sum = sum + marks[i]  
   
    elements = elements +1
   
    result = Math.floor(sum/elements)
  }
  
  return  (result)
 
}